class rpc_proxy_for_{{class_name}}
{
    public:
    {{#rpc_methods}}
    void {{rpc_name}}({{#rpc_args}}{{arg_type}} {{arg_name}} {{^last_idx}},{{last_idx}}{{/rpc_args}})
    {
        json arg_json = encode_multi({{#rpc_args}}{{arg_name}} {{^last_idx}},{{last_idx}}{{/rpc_args}});
        std::string arg_str = arg_json.to_string();
        self.owner.rpc_call({{rpc_index}}, arg_str);
    }
    {{/rpc_methods}}

}